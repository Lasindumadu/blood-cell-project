# Project demo and test instructions

This repo contains scripts to run inference, evaluate a YOLOv8 model, and basic unit tests.

Demo
----
Run the demo CLI on a single image (PowerShell):

```powershell
python .\tools\demo_cli.py --
